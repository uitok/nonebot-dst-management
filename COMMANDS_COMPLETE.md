# 命令参考文档

## 📌 命令总览（当前共 27 个）

🔒 标记为管理员权限命令。

### 房间管理
- `/dst list [页码]`
- `/dst info <房间ID>`
- `/dst start <房间ID>` 🔒
- `/dst stop <房间ID>` 🔒
- `/dst restart <房间ID>` 🔒

### 玩家管理
- `/dst players <房间ID>`
- `/dst kick <房间ID> <KU_ID>` 🔒

### 备份管理
- `/dst backup list <房间ID>`
- `/dst backup create <房间ID>` 🔒
- `/dst backup restore <房间ID> <文件名>` 🔒

### 存档管理
- `/dst archive upload <房间ID> <文件URL或文件路径>` 🔒
- `/dst archive download <房间ID>`
- `/dst archive replace <房间ID> <文件URL或文件路径>` 🔒
- `/dst archive validate <文件路径>`

### 模组管理
- `/dst mod search <关键词>`
- `/dst mod list <房间ID>`
- `/dst mod add <房间ID> <世界ID> <模组ID>` 🔒
- `/dst mod remove <房间ID> <世界ID> <模组ID>` 🔒
- `/dst mod check <房间ID>`
- `/dst mod config save <房间ID> <世界ID> --optimized` 🔒

### 控制台命令
- `/dst console <房间ID> [世界ID] <命令>` 🔒
- `/dst announce <房间ID> <消息>` 🔒

### AI 功能
- `/dst analyze <房间ID>`
- `/dst mod recommend <房间ID> [类型]`
- `/dst mod parse <房间ID> <世界ID>`
- `/dst archive analyze <文件路径>`
- `/dst ask <问题>`

---

## 📖 命令详解与示例

### 房间管理

#### `/dst list [页码]`
**功能**：查看房间列表
**权限**：所有用户
**示例**：
```
/dst list
/dst list 2
```

#### `/dst info <房间ID>`
**功能**：查看房间详情
**权限**：所有用户
**示例**：
```
/dst info 2
```

#### `/dst start <房间ID>`
**功能**：启动房间
**权限**：管理员
**示例**：
```
/dst start 2
```

#### `/dst stop <房间ID>`
**功能**：关闭房间
**权限**：管理员
**示例**：
```
/dst stop 2
```

#### `/dst restart <房间ID>`
**功能**：重启房间
**权限**：管理员
**示例**：
```
/dst restart 2
```

---

### 玩家管理

#### `/dst players <房间ID>`
**功能**：查看在线玩家
**权限**：所有用户
**示例**：
```
/dst players 2
```

#### `/dst kick <房间ID> <KU_ID>`
**功能**：踢出玩家
**权限**：管理员
**示例**：
```
/dst kick 2 KU_BQAUz1rk
```

---

### 备份管理

#### `/dst backup list <房间ID>`
**功能**：查看备份列表
**权限**：所有用户
**示例**：
```
/dst backup list 2
```

#### `/dst backup create <房间ID>`
**功能**：创建备份
**权限**：管理员
**示例**：
```
/dst backup create 2
```

#### `/dst backup restore <房间ID> <文件名>`
**功能**：恢复备份
**权限**：管理员
**示例**：
```
/dst backup restore 2 backup_2026-02-03_11-30-00.zip
```

---

### 存档管理

#### `/dst archive upload <房间ID> <文件URL或文件路径>`
**功能**：上传存档
**权限**：管理员
**示例**：
```
/dst archive upload 2 https://example.com/archive.zip
/dst archive upload 2 /path/to/archive.zip
```

#### `/dst archive download <房间ID>`
**功能**：下载存档
**权限**：所有用户
**示例**：
```
/dst archive download 2
```

#### `/dst archive replace <房间ID> <文件URL或文件路径>`
**功能**：替换存档
**权限**：管理员
**示例**：
```
/dst archive replace 2 /path/to/archive.zip
```

#### `/dst archive validate <文件路径>`
**功能**：验证存档结构
**权限**：所有用户
**示例**：
```
/dst archive validate /path/to/archive.zip
```

---

### 模组管理

#### `/dst mod search <关键词>`
**功能**：搜索模组
**权限**：所有用户
**示例**：
```
/dst mod search 健康条
```

#### `/dst mod list <房间ID>`
**功能**：查看已安装模组
**权限**：所有用户
**示例**：
```
/dst mod list 2
```

#### `/dst mod add <房间ID> <世界ID> <模组ID>`
**功能**：添加模组
**权限**：管理员
**示例**：
```
/dst mod add 2 1 1185229307
```

#### `/dst mod remove <房间ID> <世界ID> <模组ID>`
**功能**：删除模组
**权限**：管理员
**示例**：
```
/dst mod remove 2 1 1185229307
```

#### `/dst mod check <房间ID>`
**功能**：检测模组冲突与重复项
**权限**：所有用户
**示例**：
```
/dst mod check 2
```

#### `/dst mod config save <房间ID> <世界ID> --optimized`
**功能**：保存 AI 优化后的模组配置
**权限**：管理员
**示例**：
```
/dst mod config save 2 1 --optimized
```

---

### 控制台命令

#### `/dst console <房间ID> [世界ID] <命令>`
**功能**：执行控制台命令
**权限**：管理员
**示例**：
```
/dst console 2 c_announce("服务器将于10分钟后重启")
/dst console 2 1 c_regenerateworld()
```

#### `/dst announce <房间ID> <消息>`
**功能**：发送全服公告
**权限**：管理员
**示例**：
```
/dst announce 2 服务器将于10分钟后重启
```

---

### AI 功能

#### `/dst analyze <房间ID>`
**功能**：AI 配置分析
**权限**：所有用户
**示例**：
```
/dst analyze 2
```

#### `/dst mod recommend <房间ID> [类型]`
**功能**：AI 模组推荐
**权限**：所有用户
**示例**：
```
/dst mod recommend 2
/dst mod recommend 2 生存
```

#### `/dst mod parse <房间ID> <世界ID>`
**功能**：AI 模组配置解析
**权限**：所有用户
**示例**：
```
/dst mod parse 2 1
```

#### `/dst archive analyze <文件路径>`
**功能**：AI 存档分析
**权限**：所有用户
**示例**：
```
/dst archive analyze /path/to/archive.zip
```

#### `/dst ask <问题>`
**功能**：AI 智能问答
**权限**：所有用户
**示例**：
```
/dst ask 冬天基地怎么搭？
```

---

**最后更新**：2026-02-04
